services:
  - type: web
    name: guardian-capi-tool
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GUARDIAN_API_KEY
        sync: false
